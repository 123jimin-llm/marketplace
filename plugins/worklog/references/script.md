## AGENTS.md for `worklog/script/`

Agent-executable scripts for querying and maintaining the worklog.

Place project-specific scripts here. The worklog plugin provides built-in scripts for common operations (list, find-refs, archive, next-id).
