## AGENTS.md for `worklog/tag/`

Tag definitions. Each file is `{tag_name}.md` — a brief (one or two sentence) description of what the tag means.

No frontmatter. The filename is the ID. Tags are referenced by name in the `tags` frontmatter field of tasks, plans, and specs.
